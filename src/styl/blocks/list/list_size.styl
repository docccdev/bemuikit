for name, value in __bemuikit_theme.size_list
    .list_size_{name}
        margin-top: -(floor(value / 2))
        margin-bottom: -(ceil(value / 2))

        & > .list__item
            padding-top: (floor(value / 2))
            padding-bottom: (ceil(value / 2))

